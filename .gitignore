# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# IDE
.idea/
.vscode/

# Databricks Connect — artefactos locales
.databricks-connect

# Local smoke tests (no son parte del pipeline)
test_connection.py

# Scratch / temp files de sesiones de auditoría (Databricks Jobs API submits,
# notebook exports, parches temporales con TICKERS_OVERRIDE, etc.)
.audit_*

# Claude Code — instrucciones personales (no compartir)
CLAUDE.local.md
# Ignora todo bajo .claude/ MENOS las skills del proyecto (validación de datos),
# que sí se versionan/comparten. settings/agents/worktrees siguen siendo locales.
.claude/*
!.claude/skills/

# Dashboards generados (HTML autocontenidos)
fundamentals_pipeline/40_dashboards/output/

# Django web layer (web/) — local secrets, DB, and collected assets
web/.env
web/db.sqlite3
web/staticfiles/
web/media/
web/.artifact_cache/
web/dev.sqlite3

# Sistema
Thumbs.db
.DS_Store

; Web-layer test config — separate from the root pytest suite (which tests the
; fundamentals_pipeline library). Run from web/:  pytest -q
[pytest]
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = test_*.py
testpaths = tests
addopts = -q

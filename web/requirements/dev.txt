-r base.txt
pytest>=8,<9
pytest-django>=4.8,<5
pytest-cov>=5,<7
ruff>=0.6
# Static type checking for the Django code. django-stubs + drf-stubs pin a compatible mypy;
# drf-stubs (for the apps/api DRF layer) requires django-stubs>=6, so both move to the 6.x line.
mypy>=1.13,<2
django-stubs[compatible-mypy]>=6.0,<7
djangorestframework-stubs[compatible-mypy]>=3.17,<4
